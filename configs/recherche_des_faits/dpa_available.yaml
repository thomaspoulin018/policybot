version: 1
fact: dpa_available
category_arp: "Conformité légale et contractuelle"
allowed_values: ["yes", "no", "unknown"]
hint: "yes seulement lorsqu'un accord de traitement des données est explicitement disponible."
exa:
  query: "{tool} {vendor} data processing agreement DPA"
  type: auto
  num_results: 5
  include_domains: []
  contents:
    text: {max_characters: 8000}
    highlights: {query: "data processing agreement DPA", num_sentences: 3}
    summary:
      query: "Is a data processing agreement explicitly available for this offering?"
      schema:
        type: object
        additionalProperties: false
        required: [value, quote, source_url]
        properties:
          value: {type: string, enum: ["yes", "no", "unknown"]}
          quote: {type: string}
          source_url: {type: string}
selection: {strategy: source_rank, require_declared_source_url: true}
